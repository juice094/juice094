# 系统设计面试八股文

> 候选人：周景潇 | 求职方向：AI Agent 开发 / 后端开发 / 基础设施
> 项目经验：AI Agent 运行时 / 开发者工作空间编译器 / P2P 文件同步系统

---

## 1. 系统设计方法论

### 1.1 需求澄清框架

**功能性需求**：系统做什么（注册、搜索、同步、推荐...）
**非功能性需求**：
- 可用性（99.9%? 99.99%?）
- 延迟（P50/P99 < X ms?）
- 吞吐量（QPS 目标？）
- 一致性（强一致/最终一致？）
- 安全（认证/授权/加密？）

### 1.2 容量估算

```
QPS 估算：日活用户 × 人均请求数 / 86400
存储估算：用户数 × 人均数据量 × 副本数 × 预留增长
带宽估算：QPS × 平均响应大小
内存估算：热点数据 × 1.2（元数据开销）
```

### 1.3 关键概念速查

| 概念 | 一句话 | 适用场景 |
|------|--------|---------|
| **垂直扩展** | 更强的机器 | 单体、低 QPS |
| **水平扩展** | 更多的机器 | 高 QPS、无状态 |
| **数据库分片** | 按 key 分布数据 | 单库扛不住 |
| **主从复制** | 写主读从 | 读多写少 |
| **CDN** | 就近缓存 | 静态资源、大文件 |
| **消息队列** | 异步解耦 | 耗时任务、削峰 |
| **缓存** | 热点数据快速访问 | 读多、变化少 |

---

## 2. 高频系统设计题

### 2.1 设计一个 AI Agent 平台（类似 Coze/Dify）

**需求分析**：
- 功能：Agent 创建配置、工具集成、知识库、对话管理、工作流编排
- 非功能：低延迟流式响应（< 2s 首字）、多租户隔离、可用性 99.9%

**整体架构**：
```
┌─────────────────────────────────────────────┐
│                  API Gateway                 │
│         (认证/限流/路由/日志)                │
├─────────────────────────────────────────────┤
│   Agent Service  │  Knowledge Service       │
│   (Agent引擎+    │  (向量DB+                │
│    工具调用+     │   文档解析+              │
│    审批链)      │   混合搜索)              │
├─────────────────────────────────────────────┤
│   Workflow Engine│  Model Router            │
│   (DAG执行器)   │  (多Provider+            │
│                  │   负载均衡+熔断)         │
├─────────────────────────────────────────────┤
│   Conversation  │  Tool Registry           │
│   Store (SQL)   │  (MCP/内置工具)          │
└─────────────────────────────────────────────┘
```

**关键设计决策**：
- Agent 引擎：ReAct 循环 + Plan 模式双模
- 流式响应：SSE/WebSocket，首 token 到达立即推送
- 工具安全：分层审批（Interact/Smart/Yolo）+ 沙盒隔离
- 记忆系统：三层（短期上下文 + 工作记忆 + 长期向量检索）
- 多租户：每个租户独立的 Agent 配置和知识库

### 2.2 设计一个代码语义搜索引擎

**需求**：自然语言搜索代码、符号跳转、相似代码推荐

**索引 Pipeline**：
```
代码仓库 → tree-sitter 解析 → 符号提取 → 
├─ 全文索引 (Tantivy/ES BM25) → 关键词搜索
└─ 向量嵌入 (Embedding Model) → 语义搜索
→ RRF 融合 → 搜索结果
```

**增量更新**：git hook → 只重索引变化的文件

**搜索流程**：
1. 用户输入自然语言查询
2. 查询 embedding → 向量检索 Top 20
3. 查询 → BM25 分词 → 全文检索 Top 20
4. RRF 融合 → 返回 Top 10
5. 高亮匹配行 + 文件预览

### 2.3 设计一个 P2P 文件同步系统

**需求**：多设备自动同步、冲突解决、离线支持、端到端加密

**设备发现**：LAN UDP 广播 + STUN 公网发现 + Global Discovery Server + Relay 中继兜底

**同步流程**：
1. 文件变更触发 Scanner（SHA-256 分块哈希）
2. 生成 Index → 发送给所有已连接设备
3. 对端对比 Index → 找出缺失块 → Request
4. Response 传输块数据 + LZ4 压缩
5. SHA-256 验证 → 原子 rename
6. 冲突检测 → 三路合并

**关键设计**：
- 块级同步：比文件级更高效（修改大文件的 1% 只需传 1%）
- 自适应并发：根据 RTT 调整并行块请求数
- 版本向量：追踪文件历史，用于冲突解决

### 2.4 设计一个 MCP 工具网关

**需求**：统一管理多个 MCP 服务、负载均衡、权限控制、安全审计

**架构**：
```
AI Agent → MCP Gateway (认证/路由/审计)
                ├→ MCP Server A (文件工具)
                ├→ MCP Server B (数据库工具)
                └→ MCP Server C (云端工具)
```

**关键功能**：
- 服务发现和健康检查
- 工具聚合（合并多个 server 的 tool list）
- 请求路由（按 tool name 分发到对应 server）
- 权限控制（Agent 身份 → 可用工具白名单）
- 审批集成（高风险工具需要额外审批）
- 审计日志（所有工具调用记录）

---

## 3. 数据库设计专题

### 3.1 索引原理

**B+Tree**：
- 所有数据在叶子节点，内部节点只存 key 用于导航
- 叶子节点通过链表连接 → 范围查询高效
- 查询复杂度 O(log_B N)，B 是分支因子

**覆盖索引**：查询的所有列都在索引中 → 不需要回表
**联合索引最左前缀**：`INDEX(a, b, c)` → 能加速 `WHERE a=?` / `a=? AND b=?`，不能加速 `WHERE b=?`

### 3.2 事务隔离级别

| 级别 | 脏读 | 不可重复读 | 幻读 |
|------|------|----------|------|
| Read Uncommitted | 是 | 是 | 是 |
| Read Committed | 否 | 是 | 是 |
| Repeatable Read | 否 | 否 | 是(MySQL: 否) |
| Serializable | 否 | 否 | 否 |

**脏读**：读到未提交的事务的修改
**不可重复读**：同一事务内两次读取结果不同（被其他事务 update 提交）
**幻读**：同一事务内两次范围查询结果不同（被其他事务 insert 提交）

### 3.3 MVCC 原理

Multi-Version Concurrency Control：每行数据有多个版本。读操作看到事务开始时的快照，写操作创建新版本。读写互不阻塞。

PostgreSQL 用事务 ID (XID) 判断可见性，MySQL InnoDB 用 undo log + Read View。

---

## 4. 分布式系统专题

### 4.1 CAP 理论

一个分布式系统最多同时满足三项中的两项：
- **C (Consistency)**：所有节点同一时刻数据一致
- **A (Availability)**：每个请求都收到响应（不保证数据最新）
- **P (Partition Tolerance)**：网络分区时系统继续工作

实际选择：P 必须选（网络分区不可避免），在 C 和 A 之间权衡。

### 4.2 一致性模型

| 模型 | 含义 | 典型系统 |
|------|------|---------|
| 强一致 | 写入后所有读返回最新值 | Paxos/Raft, Zookeeper |
| 最终一致 | 一段时间后所有副本一致 | DNS, Cassandra |
| 因果一致 | 有因果关系的操作按序可见 | DynamoDB, CRDT |

### 4.3 分布式锁

**Redis 实现**：`SET lock_key unique_id NX EX 30`（原子设置 + 过期时间）
释放时检查 unique_id，避免误删别人的锁。

**Redlock**：在 N 个独立 Redis 实例上获取锁，超过半数成功才认为获取成功。争议：Martin Kleppmann 认为不够安全。

---

## 5. 性能优化专题

### 5.1 缓存策略

| 策略 | 读 | 写 |
|------|----|-----|
| Cache-Aside | 读 cache → miss → 读 DB → 写 cache | 写 DB → 失效 cache |
| Read-Through | 读 cache → miss → cache 自动读 DB | 不适用 |
| Write-Through | — | 写 cache → cache 同步写 DB |
| Write-Behind | — | 写 cache → 异步写 DB |

### 5.2 缓存三大问题

- **穿透**：查不存在的数据，每次都打到 DB → 布隆过滤器 / 缓存空值
- **击穿**：热点 key 过期，大量请求打到 DB → 互斥锁 / 永不过期 + 异步更新
- **雪崩**：大量 key 同时过期 → 随机 TTL / 多级缓存 / 限流降级

---

## 6. 安全专题

### API 认证与授权

- **JWT**：无状态，包含 claims + 签名。适合微服务间认证。缺点：token 签发后无法主动失效
- **OAuth2**：授权码模式 → 客户端获得 access token → 访问资源服务器。第三方登录首选
- **mTLS**：双向 TLS 认证。双方各出示证书。适合服务间通信（候选人 syncthing-rust 中使用）

### 常见攻击

| 攻击 | 防御 |
|------|------|
| SQL Injection | 参数化查询，ORM |
| XSS | 输出编码，CSP |
| CSRF | CSRF Token，SameSite Cookie |
| SSRF | URL 白名单，禁用内网 IP |

---

## 7. 面试回答框架

面对系统设计题的标准回答步骤：
1. **澄清需求**（2-3 min）：问清楚功能和非功能需求
2. **容量估算**（2 min）：QPS、存储、带宽
3. **API 设计**（3 min）：定义核心 API 端点
4. **数据模型**（3 min）：核心表结构 + 索引
5. **高层设计**（5-10 min）：画架构图，解释数据流
6. **深入细节**（5 min）：选 1-2 个关键模块深入
7. **瓶颈与优化**（3 min）：总结可能的瓶颈和解决方案

---

## 8. 系统设计评分标准与加分项

| 维度 | 权重 | 低分 | 高分 |
|------|------|------|------|
| 需求澄清 | 20% | 听完题目直接画图 | 先明确 DAU/QPS/延迟/一致性要求再动手 |
| 架构合理性 | 30% | 单层架构，无容错 | 分层清晰，可水平扩展，考虑故障域 |
| 技术选型 | 20% | 只用一个数据库 | 根据场景 trade-off（SQL vs NoSQL、Redis vs Kafka） |
| 数据模型 | 15% | 缺索引，无分片 | 表结构清晰，有索引和扩展方案 |
| 沟通 | 15% | 闷头画图不交流 | 边画边解释，邀请面试官参与 |

加分项：主动估算容量数字、引用自己的项目经验、识别瓶颈并给出优化路径。

---

## 9. 候选人项目与系统设计题的映射

| 系统设计题 | 可引用的项目经验 |
|-----------|----------------|
| 设计 Agent 平台 | Clarity 的 Agent 循环 + MCP 工具系统 + 四层审批链 |
| 设计代码搜索引擎 | Devbase 的 tree-sitter + Tantivy + 向量混合搜索 + RRF 融合 |
| 设计 P2P 同步系统 | Syncthing-rust 的 BEP 协议栈 + 四种发现机制 + 块级同步 |
| 设计 MCP 网关 | Devbase 的 71 工具路由 + McpTool trait + Stability 分级 |
| 设计缓存系统 | Clarity 的 LRU 块缓存 + 分层 TTL 策略 |

---

## 10. 十条系统设计铁律

1. 先问问题再画图——确认 DAU/QPS/延迟/一致性/数据规模
2. 先画高层再深入——LB→Service→Cache→DB 分层开始
3. 数据模型先于 API——表结构对了，API 自然出来
4. 每个组件都要有理由——不要堆 Redis+Kafka+ES 只因别人用
5. 读多写少不等于一定要缓存——先看数据库能不能撑住
6. 水平扩展不等于银弹——先搞清楚瓶颈在哪
7. CAP 要具体化——不说选 AP，说分区时通过版本向量最终收敛
8. 给出数字——不只说加缓存，说命中率 95% 可降 DB 负载 20 倍
9. 承认不足——设计缺点主动说比被追问好
10. 关联自己经验——在我的 P2P 项目中，局域网发现延迟 <50ms

---

## 11. 完整设计示例——AI Code Review 系统

需求：PR diff 分析、安全检测、风格检查。50K 审查/天。延迟 <30s。

架构：GitHub Webhook → API Gateway → Review Queue (Kafka) → Review Worker Pool
Worker：读取 diff → 文件路由（Rust→clippy, Python→ruff, 通用→LLM Agent）
LLM Agent：ReAct 模式——读 diff → 聚焦到具体行 → 生成建议。先用确定性 checker 过滤，复杂问题交 LLM
结果聚合：去重 + 优先级排序（安全>Bug>风格）+ Markdown 评论生成
瓶颈优化：LLM 结果缓存、异步审查不阻塞 CI、Worker 无状态水平扩展

---

## 12. 单机到分布式——从个人项目到大系统的演进路径

| 维度 | 单机方案（当前项目） | 分布式演进 |
|------|-----------------|-----------|
| 数据存储 | SQLite 本地 | PostgreSQL + 读写分离 + 分片 |
| 全文搜索 | Tantivy 嵌入式 | Elasticsearch 集群 |
| 向量搜索 | SQLite UDF | pgvector / Qdrant 分布式 |
| 事件总线 | 进程内 SPMC | Kafka / NATS / Redis Streams |
| Agent 调度 | tokio::spawn | K8s + 消息队列 + 无状态 Worker |
| 会话管理 | 进程内 HashMap | Redis session store |
| CI/CD | GitHub Actions | ArgoCD + 蓝绿部署 |

**关键原则**：核型业务逻辑（Agent 循环、MCP 协议、Contract-First）可保持不变——架构良好的价值在于从单机到分布式只需换基础设施层。
